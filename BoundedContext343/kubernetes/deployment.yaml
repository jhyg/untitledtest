apiVersion: apps/v1
kind: Deployment
metadata:
  name: BoundedContext343
  labels:
    app: BoundedContext343
spec:
  replicas: 1
  selector:
    matchLabels:
      app: BoundedContext343
  template:
    metadata:
      labels:
        app: BoundedContext343
    spec:
      containers:
        - name: BoundedContext343
          image: username/BoundedContext343:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: '/actuator/health'
              port: 8080
            initialDelaySeconds: 10
            timeoutSeconds: 2
            periodSeconds: 5
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: '/actuator/health'
              port: 8080
            initialDelaySeconds: 120
            timeoutSeconds: 2
            periodSeconds: 5
            failureThreshold: 5
